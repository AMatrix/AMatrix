sudo: false
language: cpp
compiler: g++
os:
  - linux
#  - osx

compiler:
  - gcc
  - clang

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake -j2 --build . --target ALL_BUILD
  - ctest -j2 --output-on-failure
